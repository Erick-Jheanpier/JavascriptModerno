const producto4={
    nombre:"monitor de 20 pulgadas",
    precio:300,
    disponible:true
}

//extrae mediante destructuracion el valor del arreglo en una variable
const {nombre, precio, disponible}=producto4
console.log(nombre)
console.log(precio)
console.log(disponible)