// metodos
/*
length-> cuenta la cantidad de caracteres de un string
indeOf->método que se usa para buscar la posición (índice) de un elemento Si el elemento sí se encuentra, devuelve su índice (>= 0).
        Si no se encuentra, devuelve -1.
includes->devuelve true o false si se encuentra el texto que buscas

*/
const apellido="Perez";
console.log(producto.length)
console.log(producto.indexOf('Perez'))
console.log(producto.includes('Perez'))