//reemplazar
// replace ->reemplaza un texto
// slice para cortar

const artefacto="televisor de 20 pulgadas"
console.log(artefacto.replace('pulgadas','"'))
console.log(artefacto.slice(2,10)) //va desde el 2 hasta el 10 es el corte que hace
console.log(artefacto.charAt(0)) //solo corta un caracter de una posicion