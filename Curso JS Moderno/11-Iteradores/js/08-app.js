// for in itera sobre objetos
const pendientes3=['tarea','comer',"estudiar"]

for(let pend in pendientes3){
    console.log(pend)
}

const automovil=[{
    modelo:'camara',
    year:1980,
    motor:6.0
} 
]

for (let p in automovil){
    console.log(automovil[p])
}