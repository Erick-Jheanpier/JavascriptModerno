//or (||) hace que solo una de las condiciones se cumplan
const efect=300
const cred=1000
const disp=efect+cred
const totalPagar=600

if(efect>totalPagar || cred>totalPagar ||disp>totalPagar){
    console.log('si podemos pagar')
}